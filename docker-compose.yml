services:
  household-manager:
    build: .
    container_name: household-manager
    ports:
      - "8080:8080"
    volumes:
      - household-data:/data
    restart: unless-stopped

volumes:
  household-data:
